/**
 * FICHIER DÉPRÉCIÉ — Toutes les données proviennent désormais de Dexie IndexedDB.
 * Ce fichier est conservé vide pour éviter les erreurs d'import.
 * Les imports existants seront progressivement nettoyés.
 */

export const MOCK_DATA = {};

export function hasMockData(_url: string): boolean {
  return false;
}

export function getMockData(_url: string): null {
  return null;
}

export function isDemoMode(): boolean {
  return false;
}
