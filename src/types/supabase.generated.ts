/**
 * AUTO-GÉNÉRÉ — Ne pas modifier manuellement
 * Régénérer avec : npm run types:supabase
 *
 * Prérequis : supabase CLI + projet lié
 *   npx supabase link --project-ref <project-id>
 *   npm run types:supabase
 */

// Types will be generated when running: npm run types:supabase
// Until then, the manual types in src/types/index.ts are used.
export type {}
